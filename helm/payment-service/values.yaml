replicaCount: 2
image:
  repository: payment-service
  tag: v1
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: alb
  host: payment.example.com

serviceAccount:
  create: true
  name: payment-sa